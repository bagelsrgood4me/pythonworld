{
    "configurations": [
        {
            "name": "Docker: Python - Django",
            "type": "debugpy",
            "request": "attach",
            "port": 5678,
            "host": "localhost",
            "env": {"PYDEVD_DISABLE_FILE_VALIDATION=1": 1},
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/app"
                }
            ],
        
        }
    ]
}